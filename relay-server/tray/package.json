{
  "name": "megabonk-overlay",
  "version": "1.0.5",
  "description": "Overlay for Megabonk's ranked mod MegaLadder",
  "main": "main.js",
  "repository": "https://github.com/OxCone1/Megabonk-Overlay",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "relay:build:win": "cd .. && npm install && npm run build:win",
    "relay:build:linux": "cd .. && npm install && npm run build:linux",
    "overlay:build": "cd ../../obs-megabonk-overlay && npm install && npm run build",
    "build:tray:win": "electron-builder --win --x64 --publish never",
    "build:tray:linux": "electron-builder --linux --x64 --publish never",
    "build:tray:publish:win": "electron-builder --win --x64 --publish always",
    "build:tray:publish:linux": "electron-builder --linux --x64 --publish always",
    "dist": "npm install && npm-run-all -s relay:build:win overlay:build build:tray:win",
    "dist:publish": "npm install && npm-run-all -s overlay:build build:tray:publish:win build:tray:publish:linux"
  },
  "author": "OxCone",
  "license": "MIT",
  "devDependencies": {
    "electron": "^40.0.0",
    "electron-builder": "^26.5.0"
  },
  "dependencies": {
    "electron-updater": "^6.7.3",
    "npm-run-all": "^4.1.5"
  },
  "build": {
    "appId": "com.megabonk.overlay",
    "productName": "MegaBonk Overlay",
    "extraResources": [
      {
        "from": "../../obs-megabonk-overlay/dist",
        "to": "overlay"
      }
    ],
    "win": {
      "icon": "images/megaicon_v2.ico",
      "target": ["nsis"],
      "artifactName": "MegaBonkOverlay.exe",
      "extraResources": [
        {
          "from": "../dist/MegaBonkRelay.exe",
          "to": "MegaBonkRelay.exe"
        }
      ]
    },
    "mac": {
      "icon": "images/megaicon_v2.icns"
    },
    "linux": {
      "icon": "images/megaicon_v2.png",
      "target": ["AppImage"],
      "artifactName": "MegaBonkOverlay.AppImage",
      "extraResources": [
        {
          "from": "../dist/MegaBonkRelay",
          "to": "MegaBonkRelay"
        }
      ]
    },
    "directories": {
      "output": "../dist"
    },
    "publish": {
      "provider": "github",
      "owner": "OxCone1",
      "repo": "Megabonk-Overlay",
      "releaseType": "release"
    }
  }
}